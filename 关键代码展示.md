# 房树人拼图游戏 - 关键代码展示

## 1. 后端核心 - Flask API 服务

### AI 心理分析报告生成接口
**文件位置：** `backend/app.py` (第 28-67 行)

```python
# 初始化 Flask 应用和跨域支持
app = Flask(__name__)
CORS(app)  # 允许跨域请求

# 初始化拼图引擎（服务端权威状态管理）
puzzle_engine = PuzzleEngine()

# 初始化行为分析存储（SQLite 数据库）
analytics_store = AnalyticsStore(
    db_path=os.path.join(os.path.dirname(__file__), "data", "behavior_analytics.db")
)

# DeepSeek AI 客户端初始化
def get_client():
    """获取或创建 OpenAI 兼容客户端，连接 DeepSeek API"""
    global client
    if client is None:
        client = OpenAI(
            api_key=os.environ.get("DEEPSEEK_API_KEY"),
            base_url="https://api.deepseek.com"
        )
    return client

# 阿里云百炼客户端（用于图片三要素校验）
def get_bailian_client():
    """获取或创建阿里云百炼客户端，用于多模态图片识别"""
    global bailian_client
    if bailian_client is None:
        api_key = os.environ.get("BAILIAN_API_KEY")
        if not api_key:
            raise RuntimeError("未配置 BAILIAN_API_KEY")
        bailian_client = OpenAI(
            api_key=api_key,
            base_url="https://dashscope.aliyuncs.com/compatible-mode/v1"
        )
    return bailian_client
```

### 系统提示词 - 心理分析规则
**文件位置：** `backend/app.py` (第 69-99 行)

```python
SYSTEM_PROMPT = """
# 角色定位
你是高校心理健康教育场景中的心理学科普助手，服务对象是大学生。
你的工作是基于房树人主题拼图行为数据，给出温和、可执行、非诊断的心理观察与自我觉察建议。

# 核心任务
我会给你"本次拼图行为数据"和"近期行为数据摘要"。
请你输出一份面向大学生的心理分析报告，覆盖学业任务、求职实习、人际协作、作息管理等校园常见压力情境。

# 证据使用规则
1. 以"本次拼图行为数据"为主证据，"近期行为数据摘要"为辅证据。
2. 当本次数据与近期趋势冲突时，优先解释本次数据，并把历史信息写成"可能的背景趋势"。
3. 当近期样本偏少时，明确提示"样本有限"，禁止下绝对结论。
4. 每条判断都要映射到行为证据（用时、步数、间隔、修改次数、顺序、回退等）。

# 安全与表达限制
1. 禁止医学诊断、病理标签或治疗结论。
2. 禁止"你有问题""你患有"等负向定性表达。
3. 只能做心理科普和自我觉察引导，不替代专业诊疗。
4. 禁止使用表情符号和无关特殊符号，中文表达要清晰整洁。
5. 仅分析两类图像：系统内置图像，或已通过"房+树+人三要素"校验的用户上传图像。
"""
```

---

## 2. 拼图引擎 - 服务端状态机

### 核心数据结构
**文件位置：** `backend/puzzle_engine.py` (第 14-48 行)

```python
# 拼图碎片数据类（不可变）
@dataclass(frozen=True)
class Piece:
    piece_id: str        # 碎片唯一标识，如 "p-0-1"
    original_row: int    # 原始行位置
    original_col: int    # 原始列位置

# 拼图行为指标（用于心理分析）
@dataclass
class PuzzleMetrics:
    piece_order: List[str] = field(default_factory=list)          # 放置顺序
    placed_once: Set[str] = field(default_factory=set)            # 已放置碎片集合
    action_timestamps: List[float] = field(default_factory=list)  # 操作时间戳
    time_intervals: List[float] = field(default_factory=list)     # 操作间隔
    modification_count: int = 0                                   # 修改次数

# 拼图游戏完整状态
@dataclass
class PuzzleGameState:
    game_id: str                    # 游戏唯一标识
    image_source: str               # 图片来源
    grid_size: int                  # 网格大小（3x3, 4x4, 6x6）
    modifiers: Dict[str, bool]      # 游戏修饰符（旋转、隐藏、捣蛋鬼）
    pieces: Dict[str, Piece]        # 所有碎片字典
    board: List[Optional[str]]      # 棋盘状态（格子中的碎片ID）
    tray: List[str]                 # 托盘中的碎片ID列表
    hidden_pool: Set[str]           # 隐藏池中的碎片ID
    rotations: Set[str]             # 被旋转的碎片ID集合
    started_at: float               # 游戏开始时间戳
    game_state: str = "playing"     # 游戏状态（playing/completed）
    move_count: int = 0             # 移动次数
    history: List[Dict] = field(default_factory=list)  # 操作历史（用于撤销）
    metrics: PuzzleMetrics = field(default_factory=PuzzleMetrics)  # 行为指标
```

### 拼图引擎核心逻辑
**文件位置：** `backend/puzzle_engine.py` (第 51-80 行)

```python
class PuzzleEngine:
    """服务端拼图引擎：前端只渲染，状态与规则统一在后端维护。"""

    def __init__(self, max_games: int = 200, ttl_seconds: int = 7200, max_undo: int = 300):
        self.max_games = max_games          # 最大游戏数量
        self.ttl_seconds = ttl_seconds      # 游戏过期时间（2小时）
        self.max_undo = max_undo            # 最大撤销步数
        self.games: Dict[str, PuzzleGameState] = {}  # 游戏状态字典
        self.random = random.SystemRandom()  # 安全随机数生成器
        self.lock = threading.RLock()        # 线程安全锁

    def create_game(self, image_source: str, grid_size: int, modifiers: Dict[str, Any]) -> Dict[str, Any]:
        """创建新游戏，返回初始状态"""
        with self.lock:
            # 清理过期游戏
            self._cleanup_expired_games()
            
            # 验证网格大小
            size = self._validate_grid_size(grid_size)
            
            # 构建碎片并打乱
            pieces = self._build_pieces(size)
            piece_ids = list(pieces.keys())
            shuffled = piece_ids[:]
            self.random.shuffle(shuffled)
            
            # 根据难度设置旋转和隐藏数量
            rotation_count = 3 if size >= 6 else 1
            hidden_count = 4 if size >= 6 else 1
            
            # 返回游戏状态给前端
            return self._serialize_state(game_state)
```

---

## 3. 行为分析存储 - SQLite 数据持久化

### 客户端标识归一化
**文件位置：** `backend/analytics_store.py` (第 11-46 行)

```python
def normalize_client_id(raw_client_id: Any) -> str:
    """归一化 client_id，确保可用于统计分组。"""
    if not isinstance(raw_client_id, str):
        return "anonymous"
    cleaned = raw_client_id.strip()
    if not cleaned:
        return "anonymous"
    # 过滤非法字符，只保留字母数字和常见符号
    cleaned = re.sub(r"[^a-zA-Z0-9._:-]", "_", cleaned)
    if len(cleaned) > 128:
        cleaned = cleaned[:128]
    return cleaned or "anonymous"

def parse_completion_time_to_seconds(value: Any) -> Optional[int]:
    """解析完成时间字符串（支持 mm:ss 或 hh:mm:ss 格式）"""
    if isinstance(value, (int, float)):
        return max(0, int(value))
    
    if not isinstance(value, str):
        return None
    
    txt = value.strip()
    parts = txt.split(":")
    
    # 验证所有部分都是数字
    if not all(p.isdigit() for p in parts):
        return None
    
    # 支持 mm:ss 格式
    if len(parts) == 2:
        mm, ss = [int(p) for p in parts]
        return max(0, mm * 60 + ss)
    
    # 支持 hh:mm:ss 格式
    if len(parts) == 3:
        hh, mm, ss = [int(p) for p in parts]
        return max(0, hh * 3600 + mm * 60 + ss)
    
    return None
```

---

## 4. 前端核心 - 游戏逻辑

### 页面结构与响应式设计
**文件位置：** `frontend/index.html` (第 1-50 行)

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>房树人拼图游戏 - 手机版</title>
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-tap-highlight-color: transparent;
            touch-action: manipulation;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            overflow-x: hidden;
        }

        /* 页面容器 - 单页应用架构 */
        .page {
            display: none;
            min-height: 100vh;
            padding: 15px;
            flex-direction: column;
        }
        .page.active {
            display: flex;
        }

        /* 主界面样式 */
        .page-home {
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .home-header {
            color: white;
            margin-bottom: 30px;
        }
        .home-header h1 {
            font-size: 1.6rem;
            margin-bottom: 8px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }
    </style>
</head>
```

### 游戏完成页面样式
**文件位置：** `frontend/index.html` (第 512-550 行)

```css
/* 完成页面样式 */
.page-complete {
    justify-content: center;
    align-items: center;
    text-align: center;
}

.complete-card {
    background: white;
    border-radius: 20px;
    padding: 30px 25px;
    max-width: 320px;
    width: 100%;
    box-shadow: 0 15px 40px rgba(0,0,0,0.15);
}

.complete-icon {
    font-size: 60px;
    margin-bottom: 15px;
}

.complete-card h2 {
    color: #10b981;
    font-size: 1.4rem;
    margin-bottom: 20px;
}

.complete-stats {
    margin-bottom: 25px;
}

.complete-stats p {
    color: #555;
    font-size: 1rem;
    margin-bottom: 8px;
}

.complete-btns {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.complete-btns .btn {
    width: 100%;
}
```

### 新手教程交互样式
**文件位置：** `frontend/index.html` (第 552-582 行)

```css
/* 新手教程遮罩层 */
.tutorial-overlay {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.7);
    z-index: 9999;
}

.tutorial-overlay.active {
    display: block;
}

/* 教程提示框 */
.tutorial-tip {
    position: fixed;
    background: white;
    color: #333;
    padding: 15px 20px;
    border-radius: 12px;
    box-shadow: 0 10px 40px rgba(0,0,0,0.3);
    font-size: 0.95rem;
    max-width: 280px;
    z-index: 10001;
    text-align: center;
}

/* 高亮元素效果 */
.tutorial-highlight {
    position: relative;
    z-index: 10000 !important;
    box-shadow: 0 0 0 4px #667eea, 0 0 20px rgba(102, 126, 234, 0.5) !important;
}
```

---

## 5. 前端核心 - 游戏逻辑

### API 客户端封装
**文件位置：** `frontend/app.js` (第 1-63 行)

```javascript
// 动态配置 API 地址（本地开发 vs 生产环境）
const API_BASE_URL = (typeof window.API_BASE_URL === 'string')
  ? window.API_BASE_URL
  : ((window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1')
    ? 'http://localhost:5000'
    : '');

// 获取或创建客户端唯一标识
function getOrCreateClientId() {
  const key = 'puzzle_client_id';
  const existing = localStorage.getItem(key);
  if (existing) return existing;

  let created = '';
  // 优先使用浏览器原生 UUID 生成
  if (window.crypto && typeof window.crypto.randomUUID === 'function') {
    created = window.crypto.randomUUID();
  } else {
    // 降级方案：时间戳 + 随机字符串
    const seed = Math.random().toString(36).slice(2);
    created = `web-${Date.now()}-${seed}`;
  }
  localStorage.setItem(key, created);
  return created;
}

// API 请求封装类
class PuzzleApi {
  constructor(baseUrl) { 
    this.baseUrl = baseUrl; 
  }

  async request(path, options = {}) {
    const res = await fetch(`${this.baseUrl}${path}`, {
      headers: { 
        'Content-Type': 'application/json', 
        ...(options.headers || {}) 
      },
      ...options,
    });
    
    let body = {};
    try { 
      body = await res.json(); 
    } catch (e) { 
      body = {}; 
    }
    
    if (!res.ok) {
      throw new Error(body.error || body.message || `请求失败(${res.status})`);
    }
    return body;
  }

  // 创建新游戏
  createGame(payload) {
    return this.request('/api/puzzle/games', { 
      method: 'POST', 
      body: JSON.stringify(payload) 
    });
  }

  // 执行游戏动作（放置、移动、旋转、撤销等）
  action(gameId, action, payload = {}, meta = {}) {
    return this.request(`/api/puzzle/games/${gameId}/actions`, {
      method: 'POST',
      body: JSON.stringify({ action, payload, ...meta }),
    });
  }
}
```

### 拼图游戏状态管理
**文件位置：** `frontend/app.js` (第 65-80 行)

```javascript
class PuzzleGame {
  constructor(api) {
    this.api = api;
    this.gameId = null;              // 服务端游戏ID
    this.serverState = null;         // 服务端返回的完整状态
    this.originalImage = null;       // 原始图片对象
    this.imageSource = '';           // 图片来源
    this.gridSize = 3;               // 网格大小
    this.modifiers = {               // 游戏修饰符
      rotation: false,               // 是否启用旋转
      hidden: false,                 // 是否启用隐藏
      trickster: false               // 是否启用捣蛋鬼
    };
    this.gameState = 'waiting';      // 游戏状态
    this.moveCount = 0;              // 移动次数
    this.startTime = null;           // 开始时间
    this.lastMetrics = {             // 最后的行为指标
      pieceOrder: [], 
      timeIntervals: [], 
      modificationCount: 0 
    };
    this.clientId = getOrCreateClientId();  // 客户端唯一标识
    this.selected = null;            // 当前选中的碎片或格子
  }
}
```

---

## 6. 图片三要素校验

### 用户上传图片必须包含"房子+树+人物"
**文件位置：** `backend/app.py` (第 212-274 行)

```python
def _prepare_image_for_bailian(mime_type, raw):
    """将上传图片压缩到百炼多模态可接受范围（最大 9MB）"""
    if len(raw) <= BAILIAN_IMAGE_MAX_BYTES:
        return mime_type, raw

    if Image is None:
        raise PuzzleError(
            f"图片大于 {BAILIAN_IMAGE_MAX_MB:g}MB，且服务端缺少压缩组件。请先压缩后重试。"
        )

    try:
        with Image.open(io.BytesIO(raw)) as img:
            # 自动旋转（根据 EXIF 信息）
            if ImageOps is not None:
                img = ImageOps.exif_transpose(img)
            img.load()

            # 处理透明通道（转为白色背景）
            if img.mode in ("RGBA", "LA") or (img.mode == "P" and "transparency" in img.info):
                rgba = img.convert("RGBA")
                base = Image.new("RGB", rgba.size, (255, 255, 255))
                base.paste(rgba, mask=rgba.split()[-1])
                img = base
            else:
                img = img.convert("RGB")

            # 限制最大边长，避免超大分辨率
            if max(img.size) > BAILIAN_IMAGE_MAX_SIDE:
                resized = img.copy()
                resized.thumbnail((BAILIAN_IMAGE_MAX_SIDE, BAILIAN_IMAGE_MAX_SIDE), Image.Resampling.LANCZOS)
                img = resized

            # 多级压缩策略：尝试不同质量和尺寸组合
            quality_levels = [90, 85, 80, 75, 70, 65, 60, 55]
            side_limits = [4096, 3072, 2560, 2048, 1600, 1280]
            best_bytes = None

            for side in side_limits:
                work = img.copy()
                if max(work.size) > side:
                    work.thumbnail((side, side), Image.Resampling.LANCZOS)

                for quality in quality_levels:
                    buff = io.BytesIO()
                    work.save(
                        buff,
                        format="JPEG",
                        optimize=True,
                        progressive=True,
                        quality=quality
                    )
                    data = buff.getvalue()
                    if best_bytes is None or len(data) < len(best_bytes):
                        best_bytes = data
                    if len(data) <= BAILIAN_IMAGE_MAX_BYTES:
                        return "image/jpeg", data

            if best_bytes and len(best_bytes) <= BAILIAN_IMAGE_MAX_BYTES:
                return "image/jpeg", best_bytes

    except PuzzleError:
        raise
    except Exception as e:
        raise PuzzleError(f"图片压缩失败：{e}") from e

    raise PuzzleError(f"图片体积仍过大，请压缩到 {BAILIAN_IMAGE_MAX_MB:g}MB 以内后重试")
```

---

## 7. 拼图游戏核心算法

### 拼图创建与碎片打乱
**文件位置：** `backend/puzzle_engine.py` (第 63-110 行)

```python
def create_game(self, image_source: str, grid_size: int, modifiers: Dict[str, Any]) -> Dict[str, Any]:
    """创建新游戏，返回初始状态"""
    with self.lock:
        # 清理过期游戏
        self._cleanup_expired_games()
        
        # 验证网格大小（3x3, 4x4, 6x6, 8x8）
        size = self._validate_grid_size(grid_size)
        normalized_modifiers = self._normalize_modifiers(modifiers)
        
        # 构建所有碎片（p-0-0, p-0-1, ...）
        pieces = self._build_pieces(size)
        piece_ids = list(pieces.keys())
        shuffled = piece_ids[:]
        self.random.shuffle(shuffled)  # 使用安全随机数打乱
        
        # 根据难度设置旋转和隐藏数量
        rotation_count = 3 if size >= 6 else 1
        hidden_count = 4 if size >= 6 else 1
        
        rotations: Set[str] = set()
        hidden_pool: Set[str] = set()
        
        # 启用旋转修饰符：随机选择碎片旋转180度
        if normalized_modifiers["rotation"]:
            rotations = set(self._sample(shuffled, min(rotation_count, len(shuffled))))
        
        # 启用隐藏修饰符：部分碎片初始不可见
        if normalized_modifiers["hidden"]:
            candidates = [pid for pid in shuffled if pid not in rotations]
            max_hidden = max(0, len(candidates) - 1)
            hidden_pool = set(self._sample(candidates, min(hidden_count, max_hidden)))
        
        # 托盘中的碎片（排除隐藏的）
        tray = [pid for pid in shuffled if pid not in hidden_pool]
        # 棋盘初始为空
        board = [None for _ in range(size * size)]
        
        # 创建游戏状态
        game_id = uuid.uuid4().hex
        state = PuzzleGameState(
            game_id=game_id,
            image_source=image_source or "",
            grid_size=size,
            modifiers=normalized_modifiers,
            pieces=pieces,
            board=board,
            tray=tray,
            hidden_pool=hidden_pool,
            rotations=rotations,
            started_at=time.time(),
        )
        
        self._validate_state(state)
        self.games[game_id] = state
        return self._serialize_state(state, message="拼图已生成，开始挑战吧")
```

### 动作分发系统
**文件位置：** `backend/puzzle_engine.py` (第 119-139 行)

```python
def apply_action(self, game_id: str, action: str, payload: Dict[str, Any]) -> Dict[str, Any]:
    """统一的动作处理入口"""
    with self.lock:
        state = self._get_game(game_id)
        self._touch(state)  # 更新最后活跃时间
        
        # 动作分发表
        dispatch = {
            "place_from_tray": self._action_place_from_tray,    # 从托盘放置
            "move_cell": self._action_move_cell,                # 格子间移动/交换
            "rotate_piece": self._action_rotate_piece,          # 旋转碎片
            "shuffle": self._action_shuffle,                    # 重新打乱
            "undo": self._action_undo,                          # 撤销操作
            "solve": self._action_solve,                        # 自动完成
            "trigger_trickster": self._action_trigger_trickster, # 触发捣蛋鬼
        }
        
        if action not in dispatch:
            raise PuzzleError(f"不支持的动作: {action}")
        
        # 执行对应动作
        result_message = dispatch[action](state, payload)
        self._validate_state(state)
        return self._serialize_state(state, message=result_message)
```

### 托盘放置逻辑
**文件位置：** `backend/puzzle_engine.py` (第 142-165 行)

```python
def _action_place_from_tray(self, state: PuzzleGameState, payload: Dict[str, Any]) -> str:
    """从托盘放置碎片到棋盘格子"""
    self._ensure_playing(state)
    piece_id = self._require_piece_id(state, payload.get("pieceId"))
    target_index = self._validate_cell_index(state, payload.get("targetIndex"))
    
    # 验证碎片在托盘中
    location, _ = self._locate_piece(state, piece_id)
    if location != "tray":
        raise PuzzleError("只能放置托盘中的碎片")
    
    # 保存历史状态（用于撤销）
    self._push_history(state)
    target_piece = state.board[target_index]
    
    # 从托盘移除
    state.tray.remove(piece_id)
    
    # 如果目标格子有碎片，交换到托盘
    if target_piece:
        state.tray.insert(0, target_piece)
        state.metrics.modification_count += 1
    
    # 放置到目标格子
    state.board[target_index] = piece_id
    self._record_piece_order(state, piece_id)
    self._record_move(state)
    
    message = "碎片已放置"
    message = self._post_action_update(state, message)
    return message
```

### 格子间移动与交换
**文件位置：** `backend/puzzle_engine.py` (第 167-193 行)

```python
def _action_move_cell(self, state: PuzzleGameState, payload: Dict[str, Any]) -> str:
    """格子间移动或交换碎片"""
    self._ensure_playing(state)
    source_index = self._validate_cell_index(state, payload.get("sourceIndex"))
    target_index = self._validate_cell_index(state, payload.get("targetIndex"))
    
    if source_index == target_index:
        raise PuzzleError("源格子与目标格子不能相同")
    
    source_piece = state.board[source_index]
    if not source_piece:
        raise PuzzleError("源格子没有可移动碎片")
    
    self._push_history(state)
    target_piece = state.board[target_index]
    
    # 目标格子有碎片：交换
    if target_piece:
        state.board[source_index], state.board[target_index] = target_piece, source_piece
        message = "格子碎片已交换"
    # 目标格子为空：移动
    else:
        state.board[target_index] = source_piece
        state.board[source_index] = None
        message = "碎片已移动到目标格子"
    
    state.metrics.modification_count += 1
    self._record_move(state)
    message = self._post_action_update(state, message)
    return message
```

### 碎片旋转逻辑
**文件位置：** `backend/puzzle_engine.py` (第 195-216 行)

```python
def _action_rotate_piece(self, state: PuzzleGameState, payload: Dict[str, Any]) -> str:
    """旋转碎片180度"""
    self._ensure_playing(state)
    if not state.modifiers.get("rotation", False):
        raise PuzzleError("当前难度未启用翻转")
    
    piece_id = self._require_piece_id(state, payload.get("pieceId"))
    location, _ = self._locate_piece(state, piece_id)
    if location not in {"tray", "board"}:
        raise PuzzleError("隐藏碎片不能翻转")
    
    self._push_history(state)
    
    # 切换旋转状态
    if piece_id in state.rotations:
        state.rotations.remove(piece_id)
        message = "碎片已恢复正向"
    else:
        state.rotations.add(piece_id)
        message = "碎片已翻转 180°"
    
    state.metrics.modification_count += 1
    self._record_move(state)
    message = self._post_action_update(state, message)
    return message
```

### 捣蛋鬼机制（随机干扰）
**文件位置：** `backend/puzzle_engine.py` (第 318-361 行)

```python
def _maybe_trigger_trickster(self, state: PuzzleGameState) -> str:
    """随机触发捣蛋鬼，打乱已放置的碎片"""
    if not state.modifiers.get("trickster", False):
        return ""
    if state.trickster_triggered:
        return ""
    if state.move_count < 5:  # 至少5步后才可能触发
        return ""
    
    placed_indices = [idx for idx, pid in enumerate(state.board) if pid is not None]
    if len(placed_indices) < 2:
        return ""
    
    # 15%概率触发
    trigger_probability = 0.15
    if self.random.random() > trigger_probability:
        return ""
    
    moved_count = self._trigger_trickster(state)
    if moved_count <= 0:
        return ""
    
    state.metrics.modification_count += moved_count
    return f"😈 捣蛋鬼出手，随机移动了 {moved_count} 个碎片"

def _trigger_trickster(self, state: PuzzleGameState) -> int:
    """执行捣蛋鬼动作：随机交换已放置的碎片"""
    placed_indices = [idx for idx, pid in enumerate(state.board) if pid is not None]
    if len(placed_indices) < 2:
        return 0
    
    # 根据难度决定移动数量
    move_count = 5 if state.grid_size >= 6 else self.random.randint(1, 3)
    move_count = min(move_count, len(placed_indices))
    selected_indices = self._sample(placed_indices, move_count)
    
    moved = 0
    for source_idx in selected_indices:
        target_candidates = [idx for idx in placed_indices if idx != source_idx]
        if not target_candidates:
            continue
        target_idx = self.random.choice(target_candidates)
        # 交换两个格子的碎片
        state.board[source_idx], state.board[target_idx] = state.board[target_idx], state.board[source_idx]
        moved += 1
    
    if moved > 0:
        state.trickster_triggered = True
    return moved
```

### 隐藏碎片渐进揭示
**文件位置：** `backend/puzzle_engine.py` (第 363-381 行)

```python
def _reveal_hidden_pieces_if_needed(self, state: PuzzleGameState) -> str:
    """当玩家完成度达到阈值时，揭示隐藏的碎片"""
    if not state.modifiers.get("hidden", False):
        return ""
    if not state.hidden_pool:
        return ""
    
    total_cells = state.grid_size * state.grid_size
    visible_cells = total_cells - len(state.hidden_pool)
    correct_count, _ = self._count_correct_cells(state)
    
    # 阈值：可见碎片数 - 1
    threshold = max(1, visible_cells - 1)
    
    if correct_count < threshold:
        return ""
    
    # 揭示所有隐藏碎片
    revealed = list(state.hidden_pool)
    self.random.shuffle(revealed)
    state.tray.extend(revealed)
    state.hidden_pool.clear()
    return f"👻 隐藏碎片出现了（{len(revealed)}块）"
```

### 状态完整性校验
**文件位置：** `backend/puzzle_engine.py` (第 384-407 行)

```python
def _validate_state(self, state: PuzzleGameState) -> None:
    """验证游戏状态的完整性和一致性"""
    total_cells = state.grid_size * state.grid_size
    if len(state.board) != total_cells:
        raise PuzzleError("棋盘尺寸异常")
    
    all_piece_ids = set(state.pieces.keys())
    board_ids = [pid for pid in state.board if pid is not None]
    tray_ids = list(state.tray)
    hidden_ids = list(state.hidden_pool)
    
    # 检查碎片是否重复
    seen = board_ids + tray_ids + hidden_ids
    if len(seen) != len(set(seen)):
        raise PuzzleError("状态异常：碎片重复出现")
    
    # 检查碎片集合是否完整
    if set(seen) != all_piece_ids:
        missing = all_piece_ids - set(seen)
        extra = set(seen) - all_piece_ids
        raise PuzzleError(f"状态异常：碎片集合不一致 missing={missing} extra={extra}")
    
    # 检查旋转集合合法性
    if not state.rotations.issubset(all_piece_ids):
        raise PuzzleError("状态异常：翻转集合含非法碎片")
    
    # 检查历史记录长度
    if len(state.history) > self.max_undo:
        raise PuzzleError("状态异常：回退历史超过限制")
```

### 完成度检测与自动完成判定
**文件位置：** `backend/puzzle_engine.py` (第 300-316 行)

```python
def _post_action_update(self, state: PuzzleGameState, message: str) -> str:
    """动作执行后的状态更新：检测完成、触发捣蛋鬼、揭示隐藏碎片"""
    trickster_message = self._maybe_trigger_trickster(state)
    reveal_message = self._reveal_hidden_pieces_if_needed(state)
    
    # 检测是否完成
    correct_count, total_cells = self._count_correct_cells(state)
    if correct_count == total_cells and not state.tray and not state.hidden_pool:
        state.game_state = "completed"
        return "拼图完成，恭喜通关"
    
    # 提示信息组合
    pieces_in_board = sum(1 for pid in state.board if pid)
    if pieces_in_board == 0 and state.tray:
        base_message = "请先放置一个碎片"
    else:
        base_message = message
    
    if reveal_message:
        base_message = f"{base_message}，{reveal_message}"
    if trickster_message:
        base_message = f"{base_message}，{trickster_message}"
    
    state.last_message = base_message
    return base_message
```

---

## 8. 前端渲染与交互

### 碎片渲染与背景定位算法
**文件位置：** `frontend/app.js` (第 286-313 行)

```javascript
createPieceElement(piece, source, cellIndex) {
  const el = document.createElement('div');
  el.className = source === 'board' ? 'puzzle-piece' : 'piece-item';
  el.dataset.pieceId = piece.id;
  el.dataset.source = source;
  if (source === 'board' && cellIndex !== null) el.dataset.cellIndex = String(cellIndex);
  el.dataset.originalRow = String(piece.originalRow);
  el.dataset.originalCol = String(piece.originalCol);
  this.applyPieceStyle(el, piece);
  this.bindPieceInteractions(el, { source, pieceId: piece.id, cellIndex });
  return el;
}

applyPieceStyle(el, piece) {
  // 计算背景图片位置（百分比定位）
  const denom = Math.max(1, this.gridSize - 1);
  const posX = (piece.originalCol / denom) * 100;
  const posY = (piece.originalRow / denom) * 100;
  
  // 设置背景图片和裁剪位置
  el.style.backgroundImage = `url(${this.imageSource})`;
  el.style.backgroundSize = `${this.gridSize * 100}% ${this.gridSize * 100}%`;
  el.style.backgroundPosition = `${posX}% ${posY}%`;
  
  // 处理旋转状态
  if (piece.rotated) {
    el.style.transform = 'rotate(180deg)';
    el.dataset.rotated = 'true';
  } else {
    el.style.transform = '';
    el.dataset.rotated = 'false';
  }
}
```

### 拖拽交互实现
**文件位置：** `frontend/app.js` (第 315-343 行)

```javascript
bindPieceInteractions(el, meta) {
  // 点击事件
  el.addEventListener('click', (e) => {
    e.stopPropagation();
    if (this.actionInFlight || this.gameState !== 'playing') return;
    this.onPieceClick(meta);
  });

  // 桌面端拖拽
  el.draggable = true;
  el.addEventListener('dragstart', (e) => {
    if (this.actionInFlight || this.gameState !== 'playing') { 
      e.preventDefault(); 
      return; 
    }
    this.dragMeta = meta;
    this.clearSelection();
    this.messageEl.textContent = '拖动到目标格子放置';
    el.classList.add('dragging');
    if (e.dataTransfer) {
      e.dataTransfer.effectAllowed = 'move';
      e.dataTransfer.setData('text/plain', meta.pieceId);
    }
  });
  
  el.addEventListener('dragend', () => {
    el.classList.remove('dragging');
    this.dragMeta = null;
    this.clearDragOver();
  });

  // 移动端触摸事件
  el.addEventListener('touchstart', (e) => this.onTouchStart(e, meta, el), { passive: false });
  el.addEventListener('touchmove', (e) => this.onTouchMove(e), { passive: false });
  el.addEventListener('touchend', (e) => this.onTouchEnd(e, meta), { passive: false });
}
```

### 触摸拖拽实现（移动端）
**文件位置：** `frontend/app.js` (第 345-399 行)

```javascript
onTouchStart(e, meta, el) {
  if (this.actionInFlight || this.gameState !== 'playing') return;
  const touch = e.touches[0];
  this.touchCtx = { 
    x: touch.clientX, 
    y: touch.clientY, 
    meta, 
    el, 
    dragging: false 
  };
}

onTouchMove(e) {
  if (!this.touchCtx || this.actionInFlight || this.gameState !== 'playing') return;
  const touch = e.touches[0];
  
  // 计算移动距离
  const dx = touch.clientX - this.touchCtx.x;
  const dy = touch.clientY - this.touchCtx.y;
  const dist = Math.sqrt(dx * dx + dy * dy);
  
  // 超过阈值才开始拖拽
  if (!this.touchCtx.dragging && dist > this.dragThreshold) {
    this.touchCtx.dragging = true;
    this.startDragVisual(touch, this.touchCtx.el);
  }
  
  if (this.touchCtx.dragging) {
    e.preventDefault();
    this.updateDragVisual(touch);
  }
}

onTouchEnd(e, meta) {
  if (!this.touchCtx) return;
  const touch = e.changedTouches[0];
  
  if (this.touchCtx.dragging) {
    e.preventDefault();
    // 检测触摸结束位置的格子
    const cell = this.getCellAtPosition(touch.clientX, touch.clientY);
    if (cell) this.handleDrop(meta, parseInt(cell.dataset.index, 10));
  } else if (!this.actionInFlight && this.gameState === 'playing') {
    // 短触摸视为点击
    this.onPieceClick(meta);
  }
  
  this.clearDragVisual();
  this.clearDragOver();
  this.touchCtx = null;
}

startDragVisual(touch, el) {
  this.clearSelection();
  this.messageEl.textContent = '拖动到目标格子放置';
  
  // 创建拖拽幽灵元素
  this.dragGhost = document.createElement('div');
  this.dragGhost.className = 'drag-ghost';
  this.dragGhost.style.backgroundImage = el.style.backgroundImage;
  this.dragGhost.style.backgroundSize = el.style.backgroundSize;
  this.dragGhost.style.backgroundPosition = el.style.backgroundPosition;
  this.dragGhost.style.width = '60px';
  this.dragGhost.style.height = '60px';
  this.dragGhost.style.left = `${touch.clientX}px`;
  this.dragGhost.style.top = `${touch.clientY}px`;
  
  if (el.dataset.rotated === 'true') {
    this.dragGhost.style.transform = 'translate(-50%, -50%) scale(1.1) rotate(180deg)';
  }
  
  el.classList.add('dragging');
  document.body.appendChild(this.dragGhost);
}
```

### 网格尺寸自适应计算
**文件位置：** `frontend/app.js` (第 265-284 行)

```javascript
updateGridDimensions() {
  // 获取原始图片尺寸
  const width = this.originalImage.naturalWidth || this.originalImage.width || 1;
  const height = this.originalImage.naturalHeight || this.originalImage.height || 1;
  const ratio = width / height;
  
  // 最大尺寸限制
  const maxW = Math.min(window.innerWidth - 40, 360);
  const maxH = 400;
  
  let w, h;
  
  // 横向图片
  if (ratio >= 1) {
    w = maxW;
    h = w / ratio;
    if (h > maxH) { 
      h = maxH; 
      w = h * ratio; 
    }
  } 
  // 纵向图片
  else {
    h = maxH;
    w = h * ratio;
    if (w > maxW) { 
      w = maxW; 
      h = w / ratio; 
    }
  }
  
  // 应用计算后的尺寸
  this.gridContainer.style.width = `${w}px`;
  this.gridContainer.style.height = `${h}px`;
}
```

---

## 技术栈总结

### 后端
- **Python 3.8+**：核心开发语言
- **Flask**：轻量级 Web 框架
- **SQLite**：行为数据持久化
- **OpenAI SDK**：统一的 AI API 调用接口
- **DeepSeek API**：心理分析报告生成
- **阿里云百炼**：图片三要素多模态识别
- **Pillow (PIL)**：图片压缩与处理

### 前端
- **原生 JavaScript**：无框架依赖
- **HTML5 + CSS3**：响应式设计
- **Fetch API**：异步网络请求
- **LocalStorage**：客户端标识持久化
- **Canvas API**：拼图渲染与交互

### 核心特性
1. **服务端权威状态**：所有游戏逻辑在后端执行，前端只负责渲染
2. **行为数据分析**：记录每次操作的时间戳、间隔、顺序、修改次数
3. **近期行为画像**：SQLite 存储 14 天内的历史数据，提炼趋势
4. **图片安全校验**：用户上传图片必须通过"房+树+人"三要素识别
5. **多级图片压缩**：自动将大图压缩到 API 可接受范围
6. **线程安全**：使用 RLock 保证并发场景下的状态一致性
